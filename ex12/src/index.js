// Only change code below this line

function randomInteger(){

   Math.floor(Math.random() * 30);
    return
    ;
}

randomInteger();

// Only change code above this line

module.expors = randomInteger;